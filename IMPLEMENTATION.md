# 区域流速看板 - 实现文档

## 概述
基于设计稿实现的区域流速看板,展示区域项目的销售流速数据,支持多维度筛选和数据分析。

## 功能模块

### 1. 概览模块 (OverviewCards)
- **位置**: 页面顶部
- **功能**:
  - 展示整体区域流速汇总数据
  - 支持按周期(当日/当月/当年)查看不同指标
  - 显示同比增长百分比
  - 点击"分类筛选"可按项目类型、拿地年份、城市等维度筛选

### 2. 趋势图模块 (TrendChart)
- **位置**: 中部
- **功能**:
  - 展示不同周期内流速变化趋势
  - 当日:显示近7日流速数据
  - 当月:显示近6周流速数据
  - 当年:显示近6个月流速数据
  - 自动计算并显示平均值

### 3. 项目明细模块 (ProjectTable)
- **位置**: 底部
- **功能**:
  - 展示具体项目的详细数据
  - 支持字段筛选和搜索功能
  - 显示协议、合同、回款等指标
  - 点击项目行可穿透至单项目流速详情

### 4. 单项目详情 (ProjectDetailModal)
- **触发**: 点击项目明细表格中的任意项目
- **功能**:
  - 展示项目基本信息(城市、拿地年份、项目类型)
  - 项目流速趋势图
  - 指标达成情况对比(协议、合同、回款)
  - 详细指标数据和达成率

### 5. 分类筛选 (ClassificationFilter)
- **触发**: 点击概览模块的"分类筛选"按钮
- **功能**:
  - 按项目类型筛选(当年首开、续销、尾盘)
  - 按拿地年份筛选(2021-2026年)
  - 按城市筛选
  - 支持多选
  - 支持重置和应用筛选

## 技术实现

### 组件结构
```
src/app/
├── App.tsx                              # 主应用组件
├── components/
│   ├── Header.tsx                       # 页面头部(周期切换、筛选器)
│   ├── OverviewCards.tsx                # 概览卡片
│   ├── TrendChart.tsx                   # 趋势图表
│   ├── ProjectTable.tsx                 # 项目明细表格
│   ├── ProjectDetailModal.tsx           # 项目详情弹窗
│   └── ClassificationFilter.tsx         # 分类筛选弹窗
```

### 使用的技术栈
- **React**: 组件化开发
- **TypeScript**: 类型安全
- **Tailwind CSS**: 样式系统
- **Recharts**: 图表可视化
- **Lucide React**: 图标库

### 数据流
1. 周期选择(当日/当月/当年)通过 Header 组件控制
2. 选择的周期传递到 OverviewCards 和 TrendChart 组件
3. 筛选条件通过 ClassificationFilter 组件设置
4. 项目点击事件触发 ProjectDetailModal 显示详情

## 交互设计

### 周期切换
- 在页面顶部选择当日/当月/当年
- 切换后自动更新概览数据和趋势图

### 筛选功能
1. **顶部筛选**: 住宅/商办/车储, 签约/协议/合同
2. **分类筛选**: 支持多维度组合筛选

### 项目穿透
- 点击项目明细表格中的任意行
- 弹出项目详情模态框
- 显示单项目的完整数据和分析

## 数据说明

### Mock 数据
当前使用模拟数据演示功能,实际使用时需要对接真实的数据接口:
- 概览数据: 套数、金额、同比
- 趋势数据: 时间序列数据
- 项目数据: 项目列表及详细指标
- 达成情况: 目标值与实际值对比

### 数据更新建议
在实际应用中,可以通过以下方式更新数据:
1. 接入 API 获取实时数据
2. 根据筛选条件请求过滤后的数据
3. 使用状态管理(如 React Context 或状态管理库)统一管理数据

## 颜色系统

### 主要颜色
- **主色**: #007440 (绿色) - 品牌色、按钮、链接
- **成功色**: #00a63e (亮绿) - 正向指标
- **警示色**: #f54900 (橙红) - 重要数据
- **文本色**: #1a1a1a (深黑) - 主要文本
- **次要文本**: #6a7282 (灰色) - 次要信息
- **背景色**: #f5f5f5 (浅灰) - 页面背景

## 响应式设计
- 最大宽度限制为 1200px
- 表格支持横向滚动处理溢出内容
- 弹窗在移动端自适应显示

## 未来扩展
1. 支持导出数据功能
2. 支持自定义图表配置
3. 支持更多维度的数据对比
4. 支持数据保存和分享
5. 支持实时数据推送
